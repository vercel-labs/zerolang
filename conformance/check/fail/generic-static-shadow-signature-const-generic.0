const N usize 4

fn needsConstGeneric<T: Type> T items ref<[N]i32> value T
  ret value

fn caller<static N: usize> i32 items ref<[N]i32>
  ret needsConstGeneric items 1

pub fn main Void
  let values [3]i32 [1, 2, 3]
  let value i32 caller (&values)
  expect (== value 1)

fn gcd u32 a u32 b u32
  mut left a
  mut right b
  while != right 0
    let next % left right
    set left right
    set right next
  ret left

pub fn main Void world World !
  if == (gcd 84 30) 6
    check world.out.write "gcd ok\n"

pub fn main Void world World !
  let text "zero language"
  mut index usize 0
  mut found - 0 1
  while < index 13
    if && (== text[index] (108 as u8)) (== found (- 0 1))
      set found index as i32
    set index + index 1
  if == found 5
    check world.out.write "string search ok\n"

type Data
  value i32

fn update Void target mutref<Data>
  set target.value + target.value 1

pub fn main Void world World !
  mut data Data Data . value 0
  let shared &data
  let observed shared.value
  update (&mut data)
  if == observed 0
    check world.out.write "borrow lexical lifetime current ok\n"

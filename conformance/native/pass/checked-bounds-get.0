pub fn main Void world World !
  let bytes [3]u8 [4, 5, 6]
  let text "zero"
  let first std.mem.get bytes 1
  let missing std.mem.get bytes 9
  let letter std.mem.get text 0
  if && (&& (&& (&& first.has (== first.value 5_u8)) (== missing.has false)) letter.has) (== letter.value (122 as u8))
    check world.out.write "checked bounds get ok\n"

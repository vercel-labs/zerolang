extern type CPoint
  x i32
  y i32

export c fn zero_add i32 a i32 b i32
  ret + a b

pub fn main Void world World !
  let point CPoint . x 2 y 3
  if == (zero_add point.x point.y) 5
    check world.out.write "c abi export ok\n"

pub fun main(world: World) -> Void raises {
    let mut buf: [256]u8 = [0; 256]
    let n = world.in.read(buf)
    if n > 0 {
        check world.out.write(buf[0..n])
    } else {
        check world.out.write("eof\n")
    }
}

fun choose(left: ref<i32>, right: ref<i32>) -> ref<i32> raises { Done } {
    raise Done
    return right
}

pub fun main() -> Void raises { Done } {
    let x = 1
    let mut y = 2
    let current = check choose(&x, &y)
    y = 3
}

type Box
  value ref<i32>

fn get ref<i32> box ref<Box>
  ret box.value

fn getMut ref<i32> box mutref<Box>
  ret box.value

pub fn main Void
  mut x 1
  let y 2
  let z 3
  mut box Box . value (&x)
  let current get (&box)
  set box.value &y
  let other getMut (&mut box)
  set box.value &z

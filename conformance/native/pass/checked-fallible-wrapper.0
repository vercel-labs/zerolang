fn inner Void flag Bool ![Bad]
  if flag
    raise Bad

fn wrapper Void !
  check inner false

pub fn main Void world World !
  check wrapper()
  check world.out.write "checked fallible wrapper ok\n"

type Holder
  value ref<i32>

fn replaceValue Bool holder mutref<Holder> next ref<i32>
  set holder.value next
  ret true

pub fn main Void
  mut old 1
  let next 2
  mut holder Holder . value (&old)
  let ok && false (replaceValue (&mut holder) (&next))
  set old 3

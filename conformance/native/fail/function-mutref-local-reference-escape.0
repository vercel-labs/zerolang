type Holder
  value ref<i32>

fn storeLocal Void holder mutref<Holder>
  let local 1
  set holder.value &local

pub fn main Void
  let fallback 0
  mut holder Holder . value (&fallback)
  storeLocal (&mut holder)

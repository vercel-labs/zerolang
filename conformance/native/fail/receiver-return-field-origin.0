type Box
  value ref<i32>
  count i32

  fn countRef ref<i32> self ref<Self>
    ret &self.count

pub fn main Void
  let x 1
  mut box Box . value (&x) count 0
  let current box.countRef()
  set box.count 1

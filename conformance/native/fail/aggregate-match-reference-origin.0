type Box
  value ref<i32>

enum Mode
  keep
  replace

pub fn main Void mode Mode
  mut x 1
  let y 2
  mut box Box Box . value (&x)
  match mode
    keep
    replace
      set box Box . value (&y)
  set x 3

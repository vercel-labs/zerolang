pub fn defaultName String
  ret "resource-cli.txt"

pub fn outputDir String
  let dir rescue (std.env.get "ZERO_RESOURCE_DIR") missing ".zero"
  ret dir

pub fn outputName String
  let name rescue (std.args.get 1) missing defaultName()
  ret name

use math
use text

pub fn main Void world World !
  mut i 0
  mut total 0
  while < i 50000
    set total + total (mix i)
    set i + i 1
  if && (> total 0) (labelOk())
    check world.out.write "module package\n"

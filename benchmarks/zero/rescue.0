fn value i32 i i32 ![Miss]
  if == (% i 4) 0
    ret i
  raise Miss

pub fn main Void world World !
  mut i 0
  mut total 0
  while < i 50000
    let item rescue (value i) err 2
    set total + total item
    set i + i 1
  if > total 0
    check world.out.write "rescue\n"

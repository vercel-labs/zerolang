use std.parse

pub fn main Void world World !
  mut i 0
  mut digits usize 0
  while < i 100000
    set digits std.parse.scanDigits "1234567890abcdef"
    set i + i 1
  if && (== digits 10) (std.parse.isIdentifierStart "_")
    check world.out.write "parse\n"
